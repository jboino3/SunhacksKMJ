# command=op   # Commented out text for if you wanted to save secret keys to one password.

# if ! [ -x "$(command -v op)" ]; then
#     command = "op.exe"
# fi

export AWS_ACCESS_KEY_ID = "AKIAX5ZI6QZBMGB4T5V2"
export AWS_SECRET_ACCESS_KEY = "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"
export AWS_REGION = "us-east-1"

# export AWS_ACCESS_KEY_ID="$(${command} item get "Terraform Basics" -- fields "Access Key ID")"
# export AWS_SECRET_ACCESS_KEY="$(${command} item get "Terraform Basics" -- fields "Access Key Secret")"
# export AWS_REGION = "$(${command} item get "Terraform Basics" -- fields "Region")"

# echo "$(${command} item get "Github" --fields "public key")" > /tmp/id_rsa.pub
# export TF_VAR_ssh_pub_key="/tmp/id_rsa.pub"